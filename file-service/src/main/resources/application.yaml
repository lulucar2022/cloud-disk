server:
  port: 8003
spring:
  application:
    name: file-service
  servlet:
    multipart:
      max-file-size: 50MB
      max-request-size: 50MB
minio:
  endpoint: http://localhost:9000
  access-key: minio
  secret-key: 88888888
  bucket-name: file-lulucar
  
